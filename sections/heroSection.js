import createSection from "../createSection/createSection";

export default(parent) =>{
  const titleString = 'Every business needs a web presence.'
  const paragraphString = 'I create websites that will help potential customers find you.'
  createSection(parent, titleString, paragraphString);
}